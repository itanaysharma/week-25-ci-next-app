name: Deploy to Staging
on:
  push:
    branches: [main]

jobs:
  redeploy_everything:
    runs-on: ubuntu-latest
    name: Deploying everything to staging cluster
    steps:
      - run: |
          echo" ${{secrets.SSH_PRIVATE_KEY}}" &> ~/ssh_key
          ssh -i ~/ssh_key ubuntu@13.201.18.99
